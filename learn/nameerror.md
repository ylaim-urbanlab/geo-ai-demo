# NameError

A `NameError` means Python tried to use a name (variable or function) that it cannot find.

This usually happens when something was not defined, not imported, or the notebook is still using an older version of code.

Why this matters here: your notebook tried to call a helper name that was not available in the active function definition.
