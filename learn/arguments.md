# Arguments

Arguments are values you pass into a function when you call it.

They let one function work with different inputs.

Why this matters here: `multiply(a, b)` uses two arguments, so you can multiply any two numbers.

Example:

```python
def multiply(a, b):
    return a * b

multiply(3, 5)  # 3 and 5 are arguments
```
