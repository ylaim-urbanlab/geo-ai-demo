# Export Formats

Export format means the file type you save your data to.

Different tools prefer different formats, so picking the right one avoids data loss and extra cleanup.

Why this matters here: ArcGIS Pro works best with File Geodatabase feature classes, while GeoJSON and CSV are useful for sharing and quick inspection.
