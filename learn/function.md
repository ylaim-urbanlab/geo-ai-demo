# Function

A function is a named block of code that does one job.

You call it by name when you want that job done, instead of rewriting the same code each time.

Why this matters here: `dice_roll()` and `square_root()` are functions in `src/tools.py`.

Example:

```python
def dice_roll():
    return 4
```
