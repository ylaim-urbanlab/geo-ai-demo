# geo-ai-demo — Python dependencies (PyPI pins)
# Frozen from maintainer env (conda env "data_science", Python 3.12). Only lines that
# install cleanly from PyPI are included (no local file:// or editable installs).
#
# Install after activating your conda env — see README.

# Core stack (src/tools.py + notebooks)
geopandas==1.1.3
pandas==3.0.2
numpy==2.4.3
pyogrio==0.12.1
pyproj==3.7.2
shapely==2.1.2
matplotlib==3.10.8
requests==2.33.1

# Notebooks
jupyterlab==4.5.6
ipykernel==7.2.0

# Often used alongside geopandas / I/O (same pins as tested env)
fiona==1.10.1
